#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *@argc: int
 *@argv: char
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	int num1, num2;
	char operator[5] = {+, -, *, /, %}

	num1 = atoi(argv[1]);
	num2 = atoi(argv[3]);
	operator = 
	if (argc != 4)
	{
		printf("%s\n", "Error");
		exit(98)
	}
	

}
