#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: nothing.
 */
void reverse_array(int *a, int n)
{
	int reverse;
	int i, ind, rev;

	for (i = 0; i < n; i++)
	{
	}

	rev=0;
	ind = n - 1;
	while (ind >= 0)
	{
		reverse[rev] = a[ind];
		rev++;
		ind--;
	}
	for (i = 0; i < n; i++)
	{
	}
}
